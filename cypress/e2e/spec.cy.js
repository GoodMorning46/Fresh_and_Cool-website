describe("empty spec", () => {
  it("passes", () => {
    cy.visit("http://localhost:3000/faq");
  });
});

describe("empty spec", () => {
  it("passes", () => {
    cy.visit("http://localhost:3000/event");
  });
});
